pluginManagement {
    repositories {
        maven { url "https://maven.fabricmc.net/" }
        maven { url "https://maven.architectury.dev/" }
        maven { url "https://maven.minecraftforge.net/" }
        //maven { url "https://maven.shedaniel.me/" }
        //maven { url "https://maven.terraformersmc.com/releases/" }
        maven { url "https://modmaven.dev/" }
        gradlePluginPortal()
    }
}

include("common")
include("fabric")
// neoforge temporarily disabled: its Loom cache locks conflict with the VS Code
// language server and break every Gradle invocation. Re-enable for Neo builds:
// include("neoforge")

rootProject.name = "immersive-aircraft-new-side"
